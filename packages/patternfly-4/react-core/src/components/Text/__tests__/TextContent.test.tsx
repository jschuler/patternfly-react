// Auto-generated do not edit

import * as React from 'react';
import { mount } from 'enzyme';
import { TextContent } from '../TextContent';

describe('TextContent test', () => {
  it('TextContent should match snapshot', () => {
    const view = mount(
      <TextContent
        children={<>ReactNode</>}
				className={"''"}
      />);
    expect(view).toMatchSnapshot();
  });
});
