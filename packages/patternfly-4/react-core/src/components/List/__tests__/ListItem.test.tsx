// Auto-generated do not edit

import * as React from 'react';
import { mount } from 'enzyme';
import { ListItem } from '../ListItem';

describe('ListItem test', () => {
  it('ListItem should match snapshot', () => {
    const view = mount(
      <ListItem
        children={"null"}
      />);
    expect(view).toMatchSnapshot();
  });
});
