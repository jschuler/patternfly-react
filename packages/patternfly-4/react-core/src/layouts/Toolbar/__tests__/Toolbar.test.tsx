// Auto-generated do not edit

import * as React from 'react';
import { shallow } from 'enzyme';
import { Toolbar } from '../Toolbar';

describe('Toolbar test', () => {
  it('Toolbar should match snapshot', () => {
    const view = shallow(
      <Toolbar
        children={<>ReactNode</>}
				className={"null"}
      />);
    expect(view).toMatchSnapshot();
  });
});
