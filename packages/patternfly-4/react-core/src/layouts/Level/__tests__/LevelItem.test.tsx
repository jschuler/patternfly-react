// Auto-generated do not edit

import * as React from 'react';
import { shallow } from 'enzyme';
import { LevelItem } from '../LevelItem';

describe('LevelItem test', () => {
  it('LevelItem should match snapshot', () => {
    const view = shallow(
      <LevelItem
        children={<>ReactNode</>}
      />);
    expect(view).toMatchSnapshot();
  });
});
