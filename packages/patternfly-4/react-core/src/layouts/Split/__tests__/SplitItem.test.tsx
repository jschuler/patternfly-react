// Auto-generated do not edit

import * as React from 'react';
import { shallow } from 'enzyme';
import { SplitItem } from '../SplitItem';

describe('SplitItem test', () => {
  it('SplitItem should match snapshot', () => {
    const view = shallow(
      <SplitItem
        isFilled={false}
				children={<>ReactNode</>}
				className={"''"}
      />);
    expect(view).toMatchSnapshot();
  });
});
