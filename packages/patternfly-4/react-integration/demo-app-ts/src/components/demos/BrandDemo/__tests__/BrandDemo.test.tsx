// Auto-generated do not edit

import * as React from 'react';
import { shallow } from 'enzyme';
import { BrandDemo } from '../BrandDemo';

describe('BrandDemo test', () => {
  it('BrandDemo should match snapshot', () => {
    const view = shallow(
      <BrandDemo
        
      />);
    expect(view).toMatchSnapshot();
  });
});
