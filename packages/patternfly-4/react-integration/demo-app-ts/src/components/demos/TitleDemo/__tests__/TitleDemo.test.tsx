// Auto-generated do not edit

import * as React from 'react';
import { shallow } from 'enzyme';
import { TitleDemo } from '../TitleDemo';

describe('TitleDemo test', () => {
  it('TitleDemo should match snapshot', () => {
    const view = shallow(
      <TitleDemo
        
      />);
    expect(view).toMatchSnapshot();
  });
});
