// Auto-generated do not edit

import * as React from 'react';
import { shallow } from 'enzyme';
import { DrawerDemo } from '../DrawerDemo';

describe('DrawerDemo test', () => {
  it('DrawerDemo should match snapshot', () => {
    const view = shallow(
      <DrawerDemo
        
      />);
    expect(view).toMatchSnapshot();
  });
});
