// Auto-generated do not edit

import * as React from 'react';
import { shallow } from 'enzyme';
import { TabsDemo } from '../TabsDemo';

describe('TabsDemo test', () => {
  it('TabsDemo should match snapshot', () => {
    const view = shallow(
      <TabsDemo
        
      />);
    expect(view).toMatchSnapshot();
  });
});
