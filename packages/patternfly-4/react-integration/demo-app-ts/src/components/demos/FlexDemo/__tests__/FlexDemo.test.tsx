// Auto-generated do not edit

import * as React from 'react';
import { shallow } from 'enzyme';
import { FlexDemo } from '../FlexDemo';

describe('FlexDemo test', () => {
  it('FlexDemo should match snapshot', () => {
    const view = shallow(
      <FlexDemo
        
      />);
    expect(view).toMatchSnapshot();
  });
});
